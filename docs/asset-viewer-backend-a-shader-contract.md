# Backend-A Shader Contract — static_prop.{vert,frag}

Generated by: `--smoke-backend-a-compile shaders`
Date: 2026-06-10
Outcome: **PASS** (compile + link succeeded)

## Minimal defs prefix (prepended before resolved source)

```glsl
#version 430
// Backend-A minimal config: legacy lane, no view-uniforms/coalesce/PBR-slots.
```

## Defines OMITTED (not injected by the spike)

- `MC2_USE_VIEW_UNIFORMS` — omitted; `u_worldToClipGL` uniform used instead of UBO
- `MC2_COALESCE` — omitted; per-draw coalesce indirection and `u_drawIDBase` absent
- `MC2_STATICPROP_PBR_SLOTS` — omitted; `u_ormTexArr` / `u_ormSampleEnable` absent
- `MC2_OBJECT_ID_BUFFER` — omitted; `layout(location=2) out uint v_objectId` absent
- `MC2_OBJECT_PARITY_CHECK` — omitted; parity writes short-circuit via `u_parityWrite=0` default

Note: `#define MC2_STATIC_PROP_LIGHTING` is defined INSIDE `static_prop.vert` at line 20 — it is part of the shader source, not the prefix.

## Full include list (in resolution order)

Vert (`static_prop.vert`):
- `shaders/include/scene.hglsl` (resolved via include/ fallback from `<scene.hglsl>`)
- `shaders/include/lighting.hglsl`

Frag (`static_prop.frag`):
- `shaders/include/render_contract.hglsl`
- `shaders/include/view_uniforms.hglsl`
- `shaders/include/material_gpu.hglsl`

## Active uniforms (from `[contract] uniform ...` output)

```
g_scene.fogStart
g_scene.fogFull
g_scene.minHazeDistance
g_scene.distanceFactor
g_scene.cameraPos
g_scene.fogColor
g_scene.baseVertexColor
u_iblSh[0]
u_worldToClipGL
u_ambientV1Strength
u_iblShStrength
u_parityWrite
u_parityBaseVertex
u_parityVertsPerType
u_parityNumLightsDebugMode
u_parityBaseLightDebugMode
u_materialFlags
u_packetID
u_maxLocalVertexID
u_tex
u_debugAddrMode
u_debugMaterialMode
u_fogValue
```

## Active SSBO blocks (from `[contract] ssbo-block ...` output)

```
LightsData    (binding 20 — declared in lighting.hglsl)
Instances     (binding 0)
PerType       (binding 2)
ParityOut     (binding 3)
Colors        (binding 1)
```

## MRT outputs (from reading static_prop.frag)

```
layout(location = 0) out vec4 FragColor;   — RGBA opaque color
layout(location = 1) out vec4 GBuffer1;    — normal-encoded + shadow-mask
```

`layout(location = 2) out uint v_objectId` exists but is guarded by `#ifdef MC2_OBJECT_ID_BUFFER` (omitted in minimal config — not reflected in the linked program).

## Resolver fixes applied in this spike (tool bugs, not shader edits)

1. `ShaderIncludeResolver.cpp`: `includePath()` now ignores `#include` tokens that appear after `//` on the same line (comment detection). The line `//   #include <include/material_gpu.hglsl>` in `material_gpu.hglsl` was falsely triggering a self-include cycle.
2. `ShaderIncludeResolver.cpp`: added `include/` fallback search when the primary `root/inc` path does not exist. `lighting.hglsl` uses `#include <scene.hglsl>` (bare name) while the engine's GL_ARB_shading_language_include mounts `shaders/include/` as a secondary root. The fallback resolves `scene.hglsl` → `shaders/include/scene.hglsl`.

No shader files were edited.

## Regenerate

```
build64/out/tools/asset_viewer/RelWithDebInfo/mc2_asset_viewer.exe --smoke-backend-a-compile shaders
```
