# MC2 Asset Viewer — Session Handoff (2026-06-03)

Self-contained pointer for a fresh session. The `mc2_asset_viewer` standalone tool grew a lot;
this is its state, how to build/run it, and what's next.

## What it is
`tools/asset_viewer/` — a standalone SDL2 + Dear ImGui + GLEW app (`mc2_asset_viewer.exe`) for
inspecting MC2 assets, decoupled from the game. Sidebar asset types; a `PreviewSurface` seam per type.

**Live types:** Textures, Materials (PBR), Static Props (model preview).
**Deferred (sidebar shows disabled):** Trees, Mechs, Vehicles, VFX, Terrain Materials, Mod Package.

## Current state
- Branch/worktree: **`claude/nifty-mendeleev`** at `…/.claude/worktrees/nifty-mendeleev` (canonical). nifty HEAD ≈ `da214e59` (moves; another perf/engine session commits here too — always re-check + merge onto latest).
- Deployed: `A:/Games/mc2-opengl/mc2-win64-v0.4/mc2_asset_viewer.exe` (current).
- All asset-viewer feature branches from the prior session are **merged + worktrees removed**. Only `claude/mc2-asset-viewer-shape-1` (pre-existing, unrelated) remains.

## Features shipped (prior session)
1. **Textures:** pluggable decoder registry (`TextureDecoder`/`TextureDecoderRegistry`); KTX2 (RGBA8 + BC7/BPTC) via `Ktx2Decoder` + `RenderCore/KtxLoader`; PNG/JPG/TGA/BMP via `LegacyImageDecoder`. **Resolution tiers** (`64/128/256/512/1024` switcher) + **fit-to-region** display (constant on-screen size; `FitTextureDisplaySize`). Default folder `data/tgl/128`.
2. **Materials (PBR):** `MaterialPreviewPBR` lit sphere (Backend B viewer-local Cook-Torrance), slot pickers (baseColor/normal/orm/emissive, sRGB/linear-correct via `MaterialTextureLoader`+stb), tangent-validated normal mapping, `.fit` material loader (`FitMaterialLoader`) with **auto-load example** on first switch + a **`.fit`-filtered picker** opening `data/defs/materials/viewer`.
3. **Static Props (model preview):** loads any of **2,030 `.tgl` props from `tgl.fst`** via the NS3 game-free loader (isolated **`mc2_tglloader` static lib** + `engine_stubs.cpp`); `TglMeshLoader` (walk shapes, group by texture, expand per-corner UVs, Stuff→GL transform, bounds); `MeshGpu` (upload + per-submesh albedo via the KTX2 decoder); `MeshPreview3D` (FBO, GL-state contained, orbit/zoom + mouse, lit shader). Controls: **3-axis model rotation** (default `-90,0,0` — see open thread), **live texture-tier swap**, **"Show lights" toggle** (hides `SpotLight_*` headlight-beam shapes; default OFF), Reset view.
4. **`docs/asset-pipeline.md`** — canonical asset inventory + loaders + resolutions + **geometry budgets** (288,708 v / 366,484 tris) + upscale/cook state + render-vs-gamedata owners + decisions (incl. in-game prop normal-mapping shelved). Drift-checked by `scripts/check-asset-pipeline-doc.sh`.
5. **tgl texture cook:** `data/tgl/{256,512,1024}` BC7 KTX2 cooked from upscaled release sources via `tools/mc2texcook/cook_tgl_tiers.py`.

## Build / run / deploy (THIS machine — non-obvious!)
- **cmake is NOT on PATH** → use `C:\mingw64\bin\cmake.exe` (the binary; compiler is MSVC VS2022 BuildTools).
- Configure (needed on first build of a worktree, or after CMakeLists changes), PowerShell, **quote the -D args**:
  ```
  & "C:\mingw64\bin\cmake.exe" -G "Visual Studio 17 2022" "-DCMAKE_PREFIX_PATH=A:/Games/mc2-opengl-src/3rdparty/3rdparty" "-DCMAKE_LIBRARY_ARCHITECTURE=x64" "-DCMAKE_POLICY_VERSION_MINIMUM=3.5" -B build64
  ```
  (`CMAKE_POLICY_VERSION_MINIMUM=3.5` is REQUIRED — mingw cmake is v4, the old SDL2 config needs it.)
- Build: `& "C:\mingw64\bin\cmake.exe" --build build64 --config RelWithDebInfo --target mc2_asset_viewer`
- Exe: `build64/out/tools/asset_viewer/RelWithDebInfo/mc2_asset_viewer.exe`. Needs `SDL2.dll`+`glew32.dll` beside it (copy from `A:/Games/mc2-opengl-src/3rdparty/3rdparty/lib/x64`).
- Deploy: copy the exe to `A:/Games/mc2-opengl/mc2-win64-v0.4/`. **It locks while running** — if the user has the viewer open, `Copy-Item` fails; ask them to close it.
- PowerShell gotchas: no `tail` (use `Select-Object -Last N`); `git ... | <unix>` breaks; `docs/superpowers` is gitignored → `git add -f` for spec/plan docs; the `tools/` dir has a broad gitignore → `git add -f` new fixture/binary files.

## Smoke suite (the test harness — `--smoke*` flags, each prints `[smoke] PASS/FAIL`, exit 0/1)
`--smoke <fixdir>` (texture), `--smoke-decoder`, `--smoke-ktx-parse`, `--smoke-ktx`, `--smoke-preview`,
`--smoke-fit` (tier fit math), `--smoke-tiers`, `--smoke-sphere`, `--smoke-backend`, `--smoke-texload`,
`--smoke-render` (material), `--smoke-tangent`, `--smoke-fit-material`, `--smoke-fit-load`,
`--smoke-tgl-load <deploy>`, `--smoke-mesh-build <deploy>`, `--smoke-mesh-render <deploy>`,
`--smoke-spotlight <deploy>`. Fixtures: `tests/fixtures/asset_viewer/` (generated by `make_fixture.py`).
Run all before merging any change; `<deploy>` = `A:/Games/mc2-opengl/mc2-win64-v0.4`.

## Working conventions (followed all prior session)
- **One feature = one worktree off latest nifty** → brainstorm/spec → writing-plans → **adversarial review** → **subagent-driven execution** (implementer + spec + code-quality gates per task) → merge to nifty → build+smoke+deploy → remove worktree. Specs/plans in `docs/superpowers/{specs,plans}/`.
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## Open threads (prioritized)
1. **Bake the model-rotation default.** The 3-axis "Model rot (deg)" defaults to `{-90,0,0}` (best guess; stands props up). The user was dialing it in — ask them for the X/Y/Z that look right on a normal building/vehicle and set it as the default in `MeshPreview3D` (`modelRotDeg_` init + Reset). May differ by prop category later.
2. **Backend A (pixel-faithful model draw).** Currently Backend B (viewer-local lit shader, "approximate" label). The RenderCore standalone init is proven (`tools/rendercore_standalone_spike` on nifty: init→view→`applyPipeline(StaticPropOpaque)`→FBO). Upgrade = promote it to a real `static_prop` mesh draw (feed the batcher buffer contract: Instances/PerType/Colors/LightsData/MaterialTable SSBOs + ViewUniforms UBO + texture array — see `gos_static_prop_batcher.cpp` finalizeGeometry/draw). Gated only on wiring, not loading.
3. **Mechs + Vehicles as their own browsable types.** Same loader works (a mech `.tgl` = base + `…X` + `SpotLight_` submeshes; the spotlight toggle already handles beams). Mechs ship many `.ase`/`.tgl` animation frames — pick a base/idle frame. Light up the deferred sidebar entries.
4. **UI/menu art BC7 cook.** Unblocked; mirror `cook_tgl_tiers.py` for the `mc2-art.zip` upscaled sources → BC7 tiers; optionally a viewer tier for art.
5. **KtxLoader >2GB fseek** — latent 32-bit `fseek` in `RenderCore/KtxLoader.cpp` (shared engine code). A task chip was spawned earlier; low practical risk.

## Cross-session dependency
The **engine-standalone / NS3** arc runs in a separate session. It delivered the game-free TGL geometry loader (see `docs/ns3-geometry-loader-standalone.md`) — fully consumed by the model preview. Backend A (#2) would consume its next deliverable (a standalone static_prop draw seam). nifty is shared — re-pull + merge onto latest before/after work.
